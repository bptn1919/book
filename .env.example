# Gemini API key (do not commit real key)
GEMINI_API_KEY=your_api_key_here

# Backend settings
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000

# Paths (relative to backend/ when running uvicorn)
DB_PATH=data/app.db
STORAGE_DIR=storage
